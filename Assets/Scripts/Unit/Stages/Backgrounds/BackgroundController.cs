using System;
using UnityEngine;

namespace Unit.Stages.Backgrounds
{
    public class BackgroundController : MonoBehaviour
    {
        private void Awake()
        {
            foreach (var VARIABLE in transform)
            {
                throw new NotImplementedException();
            }
        }
    }
}